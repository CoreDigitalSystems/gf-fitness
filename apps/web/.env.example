# GoHighLevel embedded free-trial form URL.
# When set, FreeTrialForm renders the GHL iframe instead of the placeholder form.
# Also set this in Vercel project env vars (Production, Preview, Development).
VITE_GHL_FORM_URL=
